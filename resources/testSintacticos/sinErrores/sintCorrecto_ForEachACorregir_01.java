// Prueba el uso de for y for-each

class ClaseA{

    static void forTest(){

        for(B var : x.get());
        for(V var : y());
        for(C var : new A());
        for(C var : (X) m());

    }
    
}


